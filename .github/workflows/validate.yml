name: Validate AI OS
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Check generated skill registry
        run: python scripts/generate-skill-registry.py --check
      - name: Check generated repository index
        run: python scripts/index-repository.py --check
      - name: Check generated memory index
        run: python scripts/generate-memory-index.py --check
      - name: Check generated knowledge graph
        run: python scripts/generate-knowledge-graph.py --check
      - name: Validate generated knowledge graph
        run: python scripts/validate-knowledge-graph.py
      - name: Validate memory security
        run: python scripts/validate-memory-security.py
      - name: Run unified validation
        run: python scripts/validate-all.py
      - name: Run unit tests
        run: python -m unittest discover -s tests -v
